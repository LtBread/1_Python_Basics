task_1

    Ну, наверное, здесь есть на что поругаться.

task_2

    "Основная сущность (класс) этого проекта -
    одежда, которая может иметь определённое название"
    Это из задания. Я это понял, что у экземпляра класса
    Clothes должен быть атрибут name. Хотя, как я это понимаю,
    "одежде" не за чем иметь название, а вот конкретному виду
    уже можно иметь конкретное название.

    Предполагал, что атрибут v (размер) у класса Coat соответствует
    размерной сетке (ну типа общепринятой, у меня, например по которой
    48 размер одежды (по российской). Там вроде нет промежуточных размеров,
    поэтому приводил к int. Но мои догадки не подтвердились. для 48 размера
    7.88 будет многовато. Но если предположить, что 48 это в сантиметрах
    и домножить на 0.01, то получится слишком мало... в общем ладно.
    Буду полагать что формула взята для примера.

    12 строка

    result = Clothes('Нужно материала')

    Возможно некрасиво, или вообще нельзя.

    С абстрактными классами не разобрался(

task_3

    Чуть не подорвался на сложении числа с объектом)

    Наверное make_order можно было реализовать как-то по другому,
    напрмер через for in, или while, но мне кажется, что одноразовое
    вычисление размерности "клетки" лучше цикличной печати
    "*" на экран. Наверняка есть еще какая-нибудь альтернатива.

    И ещё у меня вечная путаница: возвращать или печатать.

    Проблема этого кода, что '\n' в make_order в 21 строке
    при нулевом остатке от деления будет оставлять лишнюю пустую строку.
    Оставил так.

    На 15 строке мог поднять ValueError, но не стал, чтобы код продолжил
    работу. Рассуждал, что отрицательный результат - тоже результат.
    Но, вроде по заданию.